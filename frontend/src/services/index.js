import auth from './auth'
import me from './me'
import api from './api'
import projects from './projects'
import tasks from './tasks'

export default {
  auth,
  me,
  api,
  projects,
  tasks
}

// Или именованный экспорт:
export { auth, me, api, projects, tasks }
